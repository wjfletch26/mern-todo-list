{
  "devDependencies": {
    "concurrently": "^7.6.0",
    "gh-pages": "^5.0.0"
  },
  "homepage": "https://wjfletch26.github.io/mern-todo-list/",
  "scripts": {
    "start": "npm install && node api/server.js",
    "predeploy": "npm run build --prefix client",
    "deploy": "gh-pages -b main -d client/build",
    "build": "npm run build --prefix client",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm start\" \"npm run client\""
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
